import GlobalSearch from '@/components/modules/GlobalSearch';

export const metadata = {
  title: 'Global Search | Digitpen Hub',
  description: 'Search across all your content, contacts, projects, and more'
};

export default function GlobalSearchPage() {
  return <GlobalSearch />;
}
